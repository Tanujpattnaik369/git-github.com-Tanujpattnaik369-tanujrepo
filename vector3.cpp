#include<bits/stdc++.h>
using namespace std;
int main()
{
    //  set<int> st;
    //  int n;
    //  cin>>n;
    //  for(int i=0;i<n;i++)
    //  {int x;
    //  cin>>x;
    //  st.emplace(x);
    //  }
    //  for (auto it=st.begin();it!=st.end();it++)
    //  {
    //      cout<<*it<<" ";
    //  }
    //  st.erase(st.begin());
    //  cout<<endl;3//This also can delete only one element at atime like in case of the map.
    //  for (auto it=st.begin();it!=st.end();it++)
    //  {
    //      cout<<*it<<" ";
    //  }

// map<string,int>mp;
// map<string,int>::iterator it;
// mp.emplace("tanuj",21);
// mp.emplace("shakti",29);
// mp.emplace("pradosh",22);
// mp.emplace("devi",23);
// mp.emplace("manoj",24);
// mp.emplace("kali",25);
// it=mp.begin();
// it++;//multiple elemnts can't be deleted in the map container.
// mp.erase(it);

// for(auto it=mp.begin();it!=mp.end();it++)
// cout<<it->first<<" "<<it->second<<endl;

// map<char,int> mymap;
// map<char,int>::iterator it;

  // insert some values:
//   mymap['a']=10;
//   mymap['b']=20;
//   mymap['c']=30;
//   mymap['d']=40;
//   mymap['e']=50;
//   mymap['f']=60;

//   it=mymap.find('b');
//   mymap.erase (it);                   // erasing by iterator

//   mymap.erase ('c');                  // erasing by key

//   it=mymap.find ('e');
//   mymap.erase ( it, mymap.end() );    // erasing by range

//   // show content:
//   for (it=mymap.begin(); it!=mymap.end(); ++it)
//     cout << it->first << " => " << it->second << '\n';

//pair
// int a[]={1,2,3};
// int b[]={4,5,6};
// pair<int,int> p_array[3];
// p_array[0]={1,4};
// p_array[1]={2,5};
// p_array[2]={3,6};
// swap(p_array[0],p_array[2]);
// for(auto it=0;it<3;++it)
// cout<<p_array[it].first<<" "<<p_array[it].second<<endl;


//unordered set

// set<int> st;
// st.emplace(2);
// st.emplace(56);
// st.emplace(35);
// st.emplace(1);
// st.emplace(10);
//  auto i=st.find(56);
//  cout<<i;
// for(auto it:st){
//   cout<<it<<endl;
// }
// st.clear();
// cout<<"\nThe size of the set after deleetion\n";
// cout<<st.size();

// stack

//  stack<int> stack;
//     stack.push(21);
//     stack.push(22);
//     stack.push(24);
//     stack.push(25);
//     while (!stack.empty()) 
//     {
//         cout <<" "<< stack.top();
//         stack.pop();
        
//     }
//     cout<<"\nThe size of the stack\n";
//     cout<<stack.size();

    //   stack<int> mystack1;
    //   stack<int> mystack2;
      
    //    // pushing elements into first stack
    // mystack1.push(1);
    // mystack1.push(2);
    // mystack1.push(3);
    // mystack1.push(4);
      
    //    // pushing elements into 2nd stack
    // mystack2.push(3);
    // mystack2.push(5);
    // mystack2.push(7);
    // mystack2.push(10);
  
    //     // using swap() function to swap elements of stacks
    // mystack1.swap(mystack2);
  
    //     // printing the first stack
    // cout<<"mystack1 = ";
    //  while (!mystack1.empty()) {
    //     cout<<mystack1.top()<<" ";
    //     mystack1.pop();
    // }
  
    //     // printing the second stack
    // cout<<endl<<"mystack2 = ";
    // while (!mystack2.empty()) {
    //     cout<<mystack2.top()<<" ";
    //     mystack2.pop();
    // }

   


























  return 1;



















     
}